---
const punkter = [
  {
    tittel: 'Ledere vet det ikke fungerer',
    detalj: '– men mangler tid, kompetanse og støtte',
  },
  {
    tittel: 'HR lager rutiner og prosesser isolert',
    detalj: '– uten å involvere brukerne, ledere og ansatte',
  },
  {
    tittel: 'Arbeidsgivere spør sjelden om opplevelsen',
    detalj: '– som gjør egen kvalitet til en blindsone',
  },
];
---

<section class="monster" aria-labelledby="monster-overskrift">
  <div class="container">
    <h1 id="monster-overskrift">Mønsteret er tydelig</h1>
    <div class="monster-stat">
      <p class="stat-tekst">Kun 12 % opplever at de har fått en god start.</p>
      <div class="stat-kilde-blokk">
        <p class="stat-kilde">Kilde: GELx 2017 via HR Norge</p>
      </div>
    </div>
    <ol class="monster-liste" role="list">
      {punkter.map((punkt, i) => (
        <li class="monster-punkt">
          <span class="monster-nummer" aria-hidden="true">{i + 1}</span>
          <div class="monster-tekst">
            <h2 class="monster-tittel">{punkt.tittel}</h2>
            <p class="monster-detalj">{punkt.detalj}</p>
          </div>
        </li>
      ))}
    </ol>
  </div>
</section>

<style>
  .monster {
    padding-block: var(--section-spacing);
    background-color: var(--color-sand);
  }

  .monster h1 {
    font-family: var(--font-display);
    font-weight: var(--fw-regular);
    font-size: 3.75rem; /* 60px — hardkodet fordi --fs-h1 token ikke er pålitelig */
    line-height: var(--lh-heading);
    margin-bottom: clamp(2rem, 5vw, 4rem);
  }

  .monster-stat {
    max-width: var(--content-width);
    margin-bottom: clamp(3rem, 6vw, 5rem);
  }

  .stat-tekst {
    font-family: var(--font-body);
    font-size: var(--fs-body-large);
    line-height: var(--lh-body);
    font-weight: var(--fw-medium);
    color: var(--color-oliven-dyp);
    margin-bottom: 0.75rem;
  }

  .stat-kilde-blokk {
    border-top: 1px solid var(--color-border);
    padding-top: 1rem;
  }

  .stat-kilde {
    font-size: var(--fs-small);
    line-height: var(--lh-small);
    font-weight: var(--fw-book);
    color: var(--color-text-muted);
  }

  .monster-liste {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: clamp(2rem, 4vw, 3rem);
    max-width: 860px;
  }

  .monster-punkt {
    display: flex;
    align-items: baseline;
    gap: 1.25rem;
  }

  .monster-nummer {
    font-family: var(--font-body);
    font-weight: var(--fw-book);
    font-size: var(--fs-body-large);
    line-height: var(--lh-heading);
    color: var(--color-text-muted);
    flex-shrink: 0;
    width: 1.25rem;
    padding-top: 0.2em;
  }

  .monster-tekst {
    flex: 1;
  }

  .monster-tittel {
    font-family: var(--font-display);
    font-weight: var(--fw-regular);
    font-size: 3.333rem; /* 53.33px */
    line-height: var(--lh-heading);
    margin-bottom: 0.25rem;
  }

  .monster-detalj {
    font-family: var(--font-body);
    font-weight: var(--fw-medium);
    font-size: 1.875rem; /* 30px */
    font-style: normal;
    color: var(--color-text);
  }

  @media (max-width: 600px) {
    .monster-punkt {
      gap: 0.75rem;
    }
  }
</style>
