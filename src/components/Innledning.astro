---
---

<section class="innledning">
  <div class="container">
    <div class="innledning-inner">
      <p class="innledning-bro">Norske arbeidsgivere bruker store ressurser på å rekruttere de rette menneskene.</p>
      <p class="innledning-bro">Innfasingen overlates til tilfeldighetene.</p>
    </div>
  </div>
</section>

<style>
  .innledning {
    padding-block: var(--section-spacing);
    background-color: var(--color-sand);
  }

  .innledning-inner {
    max-width: var(--content-width);
  }

  .innledning-bro {
    font-size: var(--fs-h2);
    line-height: var(--lh-heading);
    color: var(--color-text-muted);
  }
</style>
