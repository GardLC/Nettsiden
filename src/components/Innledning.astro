---
---

<section class="innledning">
  <div class="container">
    <div class="innledning-inner">
      <p class="innledning-bro">Norske arbeidsgivere bruker store ressurser på å rekruttere de rette menneskene.</p>
      <p class="innledning-bro innledning-bro--siste">Innfasingen overlates til tilfeldighetene.</p>
    </div>
  </div>
</section>

<style>
  .innledning {
    padding-block: var(--section-spacing);
    background-color: var(--color-sand);
  }

  .innledning-bro {
    font-family: var(--font-display);
    font-weight: var(--fw-regular);
    font-size: var(--fs-h1);
    line-height: 1.0;
    letter-spacing: -0.01em;
    color: var(--color-oliven-dyp);
  }

  .innledning-bro--siste {
    margin-top: 1em;
  }
</style>
